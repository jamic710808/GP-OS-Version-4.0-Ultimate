import requests
import sys

key = "YOUR_KEY" # We will let the user paste this, or we can just see the code 401 if key is missing.
# Wait, I don't have the user's API key. I can't test it myself if I don't have the key!
